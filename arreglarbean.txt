application.properties
cerdo.nombre=Gizmo


y en la clase cerdo 

@Component
public class Cerdo implements Animal {

    @Value("${cerdo.nombre}")
    private String nombre;

    private boolean hambre;

    public Cerdo(@Value("${cerdo.nombre}") String nombre, boolean hambre) {
        this.nombre = nombre;
        this.hambre = hambre;
    }

    // Otros métodos...



 incluir la anotación @PropertySource en tu clase AppConfig para cargar el archivo de propiedades:

java
Copiar
@Configuration
@ComponentScan("com.app.tarea")
@PropertySource("classpath:application.properties")
public class AppConfig {
    // Beans